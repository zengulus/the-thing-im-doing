# Mods

Project-local mods can live here during development.

Each mod may provide a `strings.json` file with keys that override `Content/Base/strings.json`.
Mods are loaded in folder-name order after base content.

Player-installed mods may also live in `user://mods/<mod-id>/strings.json`.

